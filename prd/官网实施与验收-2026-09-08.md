# 礼链官网首版实施与验收

日期：2026-09-08。状态：本地可预览，真实飞书联调完成，尚未发布生产。

## 本次交付

官网源码已确认位于 `admin/deploy/`。原入口与当时线上 HTML 统一换行后完全一致。此次未改根目录业务后台和 `admin/official-website/` 历史工程。

首版包含六页：首页、AIGC 素材工作台、电商解决方案、城市合伙人、联系我们、隐私说明。采用深绿、暖白、少量金色，突出真实商品素材、生成效果和工作台界面。包含素材筛选、图片放大、视频播放、工作台功能切换、手机导航和预约演示弹窗。

商业表达以 AIGC 为主，标准产品与定制服务并列。运营与获客按咨询、定制范围展示。合伙人推荐客户，礼链跟进成交与交付；提交申请不代表正式授权。

客户留资和合伙人申请分别写入飞书两张表。姓名、公司、手机号、城市、需求与同意联系在对应表单采集。独立扫码联系无需先提交表单。二维码沿用现站真实图片 `images/二维码.png`；未验证接收账号的归属或企业微信身份，上线前由业务方扫码确认。系统未实现自动发送企业微信好友申请。

## 运行与维护

在 `/Users/jay/Documents/GitHub/lilianweb` 执行：

```sh
node 开发/website/build.mjs
node --test 开发/website/submissions.test.mjs
node 开发/website/server.mjs --local-cli
```

打开 http://127.0.0.1:4317/ 。当前预览服务已启动。CLI 模式依赖本机已登录的 lark-cli，仅允许回环地址及非生产环境。它会真实写入飞书，请只提交明确标记的测试数据。不带 `--local-cli` 且未提供生产凭据时，提交返回 503，不会伪装成功。

使用 Node 内置模块，无新增 npm 依赖；本机验证版本 Node 24.13.0。网页内容修改 `开发/website/build.mjs` 后重新构建；样式和交互修改 `admin/deploy/assets/site.css`、`site.js` 后也需构建，以更新资源内容哈希。生成 HTML 保留于 `admin/deploy/`。

- `开发/website/submissions.mjs`：验证、字段映射、提交去重。
- `开发/website/stores.mjs`：本地 CLI 与生产飞书应用适配器。
- `开发/website/server.mjs`：静态预览、提交 HTTP 接口、请求限制。
- `开发/website/submissions.test.mjs`：接口与上游适配器测试。
- `备份/website-2026-09-08/`：修改前的首页、robots 和 sitemap。

## 飞书与接口

飞书：[礼链官网｜客户线索与城市合伙人](https://icnnsdx1yy2u.feishu.cn/base/UFFNbJCyBa588jsGYeBcB6FunVb)。

- Base：`UFFNbJCyBa588jsGYeBcB6FunVb`
- 客户线索：`tblU9bwDkRDnk5CK`
- 城市合伙人申请：`tblasYhHd5LNAsjI`

`POST /api/website/submissions`，同源 JSON。字段：`kind` 为 lead/partner、`company`、`phone`、`name`、`city`、`solution`、`message`、`consent: true`、`source`、UUID v4 的 `submissionId`，另有反机器人隐藏字段 `website`。手机号当前支持中国大陆格式。客户端和服务端双重校验；只在飞书确认写入后返回 `{ "ok": true }`。

写入前按提交标识与内容摘要查询，串行处理上游写入，防止同一提交重试重复录入。当前假设单进程部署，不承诺多实例并发的全局唯一性。失败保留表单内容并支持重试；不返回客户资料或凭据。限制每个来源 IP 每 10 分钟 6 次请求、16KB 请求体、30 个排队提交。

## 验证记录

- 构建六页成功，浏览器脚本语法检查、git diff --check 通过。
- 13 项 Node 测试通过：字段路由、重试去重、重启后查重、内容变更、写入不确定时重试、输入校验、真实成功条件、503、来源与方法校验、限流与代理 IP、静态隔离与视频 Range、生产适配器模拟响应。
- 六页无重复 ID，27 个内部资源及链接目标 HTTP 200。
- 实际浏览器验证预约演示、合伙人申请、空表单提示、503 失败提示与输入保留、二维码弹窗、手机菜单、素材筛选、视频播放、功能页签及键盘切换。
- 375px 检查全部六页；320/768/1024/1440px 检查首页、联系和合伙人页。320px 合伙人装饰图溢出已修复并实测 scrollWidth 等于屏宽。
- 真实飞书写入并回读两条明确标识的联调记录：客户 `recvuCys71FxhQ`、合伙人 `recvuCC9sXl3jY`，均为非真实客户/申请，无需跟进。按工作规范保留，不自动删除。
- 真实联调用本地 CLI 身份；生产应用身份适配器目前只有模拟测试，未配置凭据、未验证应用授权。

## 上线前必要配置

生产发布需要用户单独确认，本次未更改 .env、密钥、CI/CD、系统配置，未提交或推送代码。

1. 确认生产飞书应用具有两张表的读取与写入权限，由服务端部署环境注入 `FEISHU_APP_ID`、`FEISHU_APP_SECRET`。不将凭据放入网页或仓库。
2. 生产模式设置 `NODE_ENV=production`。同源 HTTPS 网站将 `/api/website/submissions` 转发至 Node 服务，静态页面和后端同时发布。只上传 HTML、robots、sitemap 与所需 assets，不公开整个仓库或旧部署辅助文件。
3. 如果经过 OpenResty 代理，代理覆盖 `X-Real-IP`，服务通过 `WEBSITE_TRUSTED_PROXY_IPS` 仅信任明确的代理 IP。否则会按代理地址统一限流；禁止直接信任任意客户端转发头。
4. 以真实生产应用身份再次验证两类表单成功与失败、HTTPS 同源、视频 Range、缓存更新。服务进程守护、日志轮转由部署环境配置。
5. 业务方确认二维码接收身份、对外联系信息与隐私说明。二维码点击不等于加好友成功。

首版已预留不含个人信息的 dataLayer 事件，未连接正式分析平台，不能视作已有统计看板。飞书通知、自动分配负责人也尚未配置。

## 后续规划

第二阶段：人工确认合作关系后签发电子证书，包含证书编号、城市、授权范围、有效期与核验地址；提供 PDF/图片和授牌海报，支持到期、撤销。城市独家权益需明确约定。同期建设渠道链接与归因，不做自动佣金结算和复杂伙伴后台。

当前证书未实现。优先在首版展示与留资体验确认后推进，避免将申请误写成已获授权。
