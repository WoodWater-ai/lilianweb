# lilianweb 工作规范

## 项目边界

- 当前官网 https://www.lilianagent.cn/ 对应 `admin/deploy/index.html`；`admin/deploy/` 是官网公开文件目录。
- 根目录 `src/` 为礼链业务后台；`admin/official-website/` 与其他 HTML 为历史方案。本次官网升级不修改这些工程。
- 官网需求以 `/Users/jay/Desktop/02-projects/礼链/prd/礼链官网升级-需求共识与交接-2026-09-08.md` 和用户后续指示为准。

## 目录约定

- `prd/`：官网实施说明、接口约定、素材来源与验证记录。
- `设计/`：官网视觉参考和截图。
- `开发/`：官网服务端与验证脚本；公开网页继续使用既有 `admin/deploy/`。
- `备份/`：变更前的源码备份；不使用副本、最新版等命名。
- 服务端代码、业务凭据、内部文档和本地数据不能放进公开文件目录。

## 首版范围

- 礼链统一品牌，面向全品类电商，以 AIGC 素材工作台为主，服务方式为标准产品 + 定制服务。
- 官网展示、客户留资写入飞书、独立企微联系入口、城市合伙人申请。
- 城市合伙人负责推荐客户，礼链负责成交与交付。证书、渠道归因和核验为二期。
- 只使用可核实的案例和宣传信息，未交付能力不得标记为已上线。

## 操作规则

- 不自动 commit；不覆盖既有用户改动。
- 删除、修改 .env/密钥/token/CI/CD、数据库 schema 或迁移、git push/rebase、全局依赖/系统配置、生产发布前先询问用户。
- 不打印或提交密钥；服务端从部署环境读取凭据，浏览器不持有飞书访问凭据。
- 新目录先按此文件确定用途再创建。测试写入只使用明确标识的测试数据，不混同真实客户。
- 完成前验证页面、表单成功与失败、移动端和静态资源；以实际验证结果交付。
